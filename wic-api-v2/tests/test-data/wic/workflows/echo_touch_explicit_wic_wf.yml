steps:
- echo_string_wic_compatible:
    in:
      message: BBBC001
      # See how we need to : - create those dummy parameters in the clts
      # cannot use out: section but reference those inputs like that
      stdout_string: '&my_stdout'
- touch_single:
    in:
      filename: '*my_stdout'
    
